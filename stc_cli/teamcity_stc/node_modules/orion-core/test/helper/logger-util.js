var logger = this.logger = require('../../lib/Logger').getInstance();

logger.addHandler(function (level, message) {
    console.log(message);
});

logger.setLevel('error');

module.exports.logger = logger;

